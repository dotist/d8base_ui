footer
// omit header, b/c of dropdown menu... hm...
  overflow auto
  .region
    // overflow auto

body
  background-color COLOR_BACKGROUND
  .layout-container
    padding 50  px

code {
  background: #f4f5f6;
  border-radius: .4rem;
  margin: 0 .2rem;
  padding: .2rem .5rem;
  white-space: nowrap;
}

pre {
  background: #f4f5f6;
  border-left: 0.3rem solid #9b4dca;
  overflow-y: hidden;
}

pre > code {
  border-radius: 0;
  display: block;
  padding: 1rem 1.5rem;
  white-space: pre;
}

img
  max-width: 100%
  height auto

.field--type-image
  .field__item
    margin-bottom 20px
  a
    display block
    line-height: 0


a:hover img
  border-bottom 0px

